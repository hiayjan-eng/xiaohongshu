diff --git a/apps/extension/scripts/run-full-scan-spike.mjs b/apps/extension/scripts/run-full-scan-spike.mjs
--- a/apps/extension/scripts/run-full-scan-spike.mjs
+++ b/apps/extension/scripts/run-full-scan-spike.mjs
@@ -65,7 +65,10 @@
   const sessionId = started.session.sessionId;
   await waitForSessionStatus(page, sessionId, "paused");
   const pausedSession = await getSession(page, sessionId);
-  assert.ok(pausedSession.validCount >= 1200 && pausedSession.validCount < 3000);
+  assert.ok(
+    pausedSession.validCount >= 1200 && pausedSession.validCount < 3000,
+    JSON.stringify(pausedSession)
+  );
