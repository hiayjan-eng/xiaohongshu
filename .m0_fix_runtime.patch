diff --git a/apps/extension/src/full-scan-core.js b/apps/extension/src/full-scan-core.js
--- a/apps/extension/src/full-scan-core.js
+++ b/apps/extension/src/full-scan-core.js
@@ -23,6 +23,7 @@
       this.running = false;
       this.stopRequested = false;
       this.captureQueue = Promise.resolve();
+      this.runPromise = null;
       this.lastMutationAt = 0;
@@ -71,9 +72,8 @@
       this.installObserver();
       this.restoreScrollCheckpoint();
       await this.captureFromNodes([this.root]);
-      this.captureQueue = this.captureQueue
-        .then(() => this.runLoop())
-        .catch((error) => this.fail(error));
+      this.runPromise = this.runLoop()
+        .catch((error) => this.fail(error));
       return { ok: true, session: this.session };
diff --git a/apps/extension/src/full-scan-content.js b/apps/extension/src/full-scan-content.js
--- a/apps/extension/src/full-scan-content.js
+++ b/apps/extension/src/full-scan-content.js
@@ -41,7 +41,7 @@
   async function handleControlMessage(message) {
     switch (message.type) {
       case "M0_FULL_SCAN_GET_PAGE_STATUS":
-        return { ok: true, inspection: controller.inspectPage() };
+        return { ok: true, inspection: publicInspection(controller.inspectPage()) };
@@ -86,4 +86,10 @@
       }
     });
   }
+
+  function publicInspection(inspection) {
+    if (!inspection?.ok) return inspection;
+    const { root: _root, scrollContainer: _scrollContainer, ...serializable } = inspection;
+    return serializable;
+  }
 })();
