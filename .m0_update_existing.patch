diff --git a/.m0_patch_probe b/.m0_patch_probe
deleted file mode 100644
--- a/.m0_patch_probe
+++ /dev/null
@@ -1 +0,0 @@
-probe
diff --git a/apps/extension/src/background.js b/apps/extension/src/background.js
--- a/apps/extension/src/background.js
+++ b/apps/extension/src/background.js
@@ -1,3 +1,5 @@
+importScripts("full-scan-idb.js");
+
 const WEB_APP_URL_PATTERNS = [
   "https://xiaohongshu-green.vercel.app/*",
   "http://localhost:5173/*",
@@ -5,14 +7,55 @@ const WEB_APP_URL_PATTERNS = [
 ];
 
 chrome.runtime.onInstalled.addListener(() => {
+  void configureSidePanel();
   void reinjectOpenWebTabs();
 });
 
 chrome.runtime.onStartup?.addListener(() => {
+  void configureSidePanel();
   void reinjectOpenWebTabs();
 });
 
+void configureSidePanel();
+
+chrome.runtime.onMessage.addListener((message, _sender, sendResponse) => {
+  if (!isFullScanDatabaseMessage(message)) return false;
+  void globalThis.CollectionRevivalFullScanDb.handleMessage(message)
+    .then((response) => sendResponse(response))
+    .catch((error) => sendResponse({
+      ok: false,
+      error: error instanceof Error ? error.message : String(error)
+    }));
+  return true;
+});
+
+async function configureSidePanel() {
+  if (!chrome.sidePanel?.setPanelBehavior) return;
+  try {
+    await chrome.sidePanel.setPanelBehavior({ openPanelOnActionClick: true });
+  } catch {
+    // Older Chromium builds may expose the API before supporting this behavior.
+  }
+}
+
+function isFullScanDatabaseMessage(message) {
+  return [
+    "M0_FULL_SCAN_CREATE_SESSION",
+    "M0_FULL_SCAN_GET_SESSION",
+    "M0_FULL_SCAN_UPDATE_SESSION",
+    "M0_FULL_SCAN_PERSIST_ITEMS",
+    "M0_FULL_SCAN_VERIFY_SESSION",
+    "M0_FULL_SCAN_GET_DIAGNOSTICS",
+    "M0_FULL_SCAN_RESET_SESSION"
+  ].includes(message?.type);
+}
+
 async function reinjectOpenWebTabs() {
   const tabs = await chrome.tabs.query({ url: WEB_APP_URL_PATTERNS });
   await Promise.all(tabs.map(async (tab) => {
diff --git a/apps/extension/manifest.json b/apps/extension/manifest.json
--- a/apps/extension/manifest.json
+++ b/apps/extension/manifest.json
@@ -8,7 +8,8 @@
     "storage",
     "downloads",
-    "tabs"
+    "tabs",
+    "sidePanel"
   ],
@@ -23,8 +24,10 @@
   ],
   "action": {
-    "default_title": "收藏复活扫描",
-    "default_popup": "src/popup.html"
+    "default_title": "收藏复活全量扫描"
+  },
+  "side_panel": {
+    "default_path": "src/sidepanel.html"
   },
@@ -51,7 +54,9 @@
       ],
       "js": [
-        "src/xhs-scanner.js"
+        "src/full-scan-core.js",
+        "src/xhs-scanner.js",
+        "src/full-scan-content.js"
       ],
diff --git a/apps/extension/package.json b/apps/extension/package.json
--- a/apps/extension/package.json
+++ b/apps/extension/package.json
@@ -2,12 +2,13 @@
   "name": "@revival/extension",
   "version": "0.1.0",
   "private": true,
-  "description": "Collection Revival browser extension Beta for user-triggered visible DOM scanning.",
+  "description": "Collection Revival browser extension Beta and M0 full-scan technical spike.",
   "scripts": {
-    "typecheck": "node ./scripts/validate-extension.mjs",
+    "typecheck": "node ./scripts/validate-extension.mjs && node ./scripts/validate-full-scan-spike.mjs",
     "test:dom": "node ./scripts/validate-extension.mjs",
+    "test:full-scan-spike": "node ./scripts/run-full-scan-spike.mjs",
     "test:profiles": "node ./scripts/verify-extension-profiles.mjs",
     "build": "node ./scripts/build-extension.mjs",
     "build:preview": "node ./scripts/build-extension.mjs --preview"
diff --git a/apps/extension/scripts/build-extension.mjs b/apps/extension/scripts/build-extension.mjs
--- a/apps/extension/scripts/build-extension.mjs
+++ b/apps/extension/scripts/build-extension.mjs
@@ -48,7 +48,20 @@ if (isPreview) {
 }
 writeFileSync(resolve(outDir, "manifest.json"), `${JSON.stringify(outputManifest, null, 2)}\n`);
 
-for (const file of ["popup.html", "popup.js", "popup.css", "web-bridge.js", "xhs-scanner.js", "background.js", "build-profile.js"]) {
+for (const file of [
+  "popup.html",
+  "popup.js",
+  "popup.css",
+  "sidepanel.html",
+  "sidepanel.js",
+  "sidepanel.css",
+  "web-bridge.js",
+  "xhs-scanner.js",
+  "full-scan-core.js",
+  "full-scan-content.js",
+  "full-scan-idb.js",
+  "background.js",
+  "build-profile.js"
+]) {
   copyFileSync(resolve(root, "src", file), resolve(srcOut, file));
 }
diff --git a/apps/extension/src/full-scan-core.js b/apps/extension/src/full-scan-core.js
--- a/apps/extension/src/full-scan-core.js
+++ b/apps/extension/src/full-scan-core.js
@@ -379,7 +379,7 @@
         profileIdHash,
         favoritesPageIdentity: `${hostname}|${pathname}|tab=favorites|subtab=notes|profile=${profileIdHash}`,
         selectorVersion: SELECTOR_VERSION,
-        extensionVersion: chrome?.runtime?.getManifest?.().version || "0.2.3-spike"
+        extensionVersion: globalThis.chrome?.runtime?.getManifest?.().version || "0.2.3-spike"
       },
       diagnostics: {
diff --git a/.m0_update_existing.patch b/.m0_update_existing.patch
deleted file mode 100644
--- a/.m0_update_existing.patch
+++ /dev/null
@@ -1,999 +0,0 @@
